// AUTO-GENERATED OPEN SHIM
// Keeps server.mjs bootable in open distribution.
// Source: packages/open-core/src/core/safety.mjs (if present)

import * as M from "../../packages/open-core/src/core/safety.mjs";

function _openStub(name, args) {
  return { ok: false, mode: 'open_stub', name, args, reason: 'Not available in Open Core (requires paid-platform or missing in this snapshot).' };
}

export const WebSocketServer } from 'ws';
import pty from 'node-pty';
import OpenAI from 'openai';

import { isDangerousBash = M.WebSocketServer } from 'ws';
import pty from 'node-pty';
import OpenAI from 'openai';

import { isDangerousBash ?? class WebSocketServer } from 'ws';
import pty from 'node-pty';
import OpenAI from 'openai';

import { isDangerousBash {
  constructor(...args) { this._open_stub = true; this._args = args; }
};

export * from "../../packages/open-core/src/core/safety.mjs";
